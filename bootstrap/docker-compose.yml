version: "3"
services:

  # components Worker
  components:
    extends:
      file: components.yml
      service: component-jars

  # # Node Workers
  # react-js:
  #   extends:
  #     file: node-installs.yml
  #     service: react-js
  # react-ts:
  #   extends:
  #     file: node-installs.yml
  #     service: react-ts

  # certgen Worker
  certgen:
    extends:
      file: certgen.yml
      service: certgen

  # Tomcat Worker
  tomcat:
    extends:
      file: tomcat-wars.yml
      service: tomcat
